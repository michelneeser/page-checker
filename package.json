{
  "name": "page-checker",
  "version": "1.0.0",
  "description": "small tool to check if a website has changes in content",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michel Neeser",
  "license": "ISC",
  "dependencies": {
    "chrome-aws-lambda": "^2.0.2",
    "puppeteer-core": "^2.0.0"
  },
  "devDependencies": {
    "puppeteer": "^2.0.0"
  }
}